// Metadata for the planning page
export const metadata = {
    title: "Plan a Trip - Ariel's Travel Planner",
    description: "Generate cycling and trekking routes with AI",
};

export default function PlanningLayout({ children }) {
    return children;
}